"""Geometric understanding of LiDAR point clouds (Phase 2).

Algorithm modules (``ransac``, ``ground``, ``clustering``, ``normals``,
``bounding_boxes``, ``surface_analysis``, ``pipeline``) import no ROS code and
can be used on saved frames. ``geometric_analysis_node`` and ``markers`` are
the ROS layer.
"""

__version__ = '0.1.0'
